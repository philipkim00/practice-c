사용자 친화적이지않다.

#include <stdio.h>  = 
//보기좋게하기위해 비운다. 코드 덩어리 비교할떄 사용.
int main(vold) 
{
	printf("hello, world\n");
}

해석
int main(vold) = 시작하는 신호.? 시작하는 역할 , 모든 코드가 중괄호 사이에 들어간다.
{

}

print(                 ) 
printf(                 ) 
c에서는 'say'와 같은 함수가 없어서 텍스트형인 printf씀

printf("hello. world" );
출력하고싶은 텍스트를 쌍따옴표로 감싸야합니다.

가장까먹기 쉬운것은 
;(세미콜론)=코드 각줄에 마침표에 붙인다.

#include <stdio.h>  
stdio.h 라는 파일이 무엇이든 함수에 접근하기 위해 그 파일을 들여다보라는 의미.

터미널창 의미
일종의 옛날 인터페이스 프롬포트 
초기 언어들은 명령어 입렵방식을 채택.

printf는 '말해라' 블록과 비슷



source code(c,파이썬,자바,ㅊc++) -> ㅁㅁㅁㅁ -> machine code(2진법 0과1로 이루어진 코드)
                                               compiler(컴파일러)
컴파일러 :번역을 수행하는 알고리즘 혹은 소프트웨어가 필요함.

clang hello.c (clang이 컴파일러)

a.out (2진법으로 만들어진 오브젝트코드.머신코드)
.
./a.out 디렉토리에서 a.out 라는 파일실행해라.

$=프롬포트 

\n= 컴퓨터로 하여금 줄을 바꾸도록하는 옛날사람들에 약속.

컴퓨터는 시킨일만을 하기때문에 잘 시키자 ㅋㅋ.

-o hello =컴파일러할떄 hello파일로 컴파일러됨.
ls(list)

rm(파일 지우는 명령어) 

mkdir (풀더만드는 명령어)

rmdir(풀더지우는 명령어)

모든 명령어를 외울 필요x
그렇지만

마우스와 그래픽 UI로 할 수 있는 모든 일을 명령어로 할수 있다.
